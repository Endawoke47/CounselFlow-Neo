# Ultra-simple Dockerfile for immediate deployment
FROM node:20-alpine

WORKDIR /app

# Copy only what we need
COPY package*.json ./
COPY . .

# Install dependencies
RUN npm install

# Build the app
RUN npm run build

# Expose port
EXPOSE 3000

# Start the app
CMD ["npm", "start"]
